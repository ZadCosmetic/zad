const toggler = document.querySelector(".navbar-toggler"), navBar = document.querySelector("#navbar"), listNav = document.querySelector(".navbar-collapse"), sections = document.querySelectorAll(".contact,.about"), options = { root: null, rootMargin: "0px 0px 10px 0px", threshold: .3 }; toggler.addEventListener("click", e => { navBar.classList.toggle("show") }), listNav.addEventListener("click", () => { navBar.classList.remove("show") }); const observer = new IntersectionObserver(e => { e.forEach(e => { e.isIntersecting ? e.target.classList.add("active-sec") : e.target.classList.contains("active-sec") && e.target.classList.remove("active-sec") }) }, options); sections.forEach(e => observer.observe(e));